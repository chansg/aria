# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environment
venv/
.venv/
env/

# Environment & secrets
.env
config.py

# PyCharm
.idea/

# Aria data (private user data — never commit)
data/memory.db
data/memory.db-shm
data/memory.db-wal
data/calendar.db
data/usage_log.json
data/voice_samples/
data/voice_profile.json
data/web_cache.json
data/*token*.txt
data/*token*.json
data/captures/

# Market analyst — daily snapshots regenerated each fetch
data/market/*.json

# Large model files (re-download, don't commit)
assets/models/*.onnx

# Piper voice models (large binary — download separately)
assets/voices/*.onnx
assets/voices/*.onnx.json
assets/voices/**/*.onnx
assets/voices/**/*.onnx.json
assets/voices/**/*.bin

# Audio outputs
assets/sounds/aria_response.wav

# Claude Code local settings
.claude/

# Session logs — never commit
logs/

# OS
.DS_Store
Thumbs.db
